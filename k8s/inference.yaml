apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: estateai-predictor
  namespace: estateai-prod
spec:
  predictor:
    serviceAccountName: sa-s3-access
    sklearn:
      storageUri: s3://estateai-models/models/house_model.pkl
